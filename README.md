# Smart Timetable Scheduler
Resume stack: Graph Coloring + HTML/CSS/JavaScript.

Run: simply open `index.html` in a browser, click Load Demo, then Generate Timetable.

Algorithm: each class is a graph vertex; edges represent teacher/room/explicit conflicts; greedy graph coloring assigns different colors to adjacent vertices; colors map to timetable slots.
